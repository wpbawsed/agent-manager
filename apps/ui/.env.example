# UI — 環境變數範本
# 複製為 .env 並填入實際值（本機 dev 用；container 版走 entrypoint.sh 的 runtime 注入）

# 後端 API 位置（含 scheme，不含結尾斜線，例如 https://your-api.up.railway.app）
# 留空 = 呼叫相對路徑 /api，由 vite dev proxy（本機）或 nginx.conf（docker-compose）轉送到後端
VITE_BACKEND_HOST=
